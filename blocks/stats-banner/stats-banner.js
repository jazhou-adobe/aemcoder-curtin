export default async function decorate(block) {
  // Structure is already correct: first row is title, remaining rows are stat cards
  // Each stat card row has: [icon cell, text cell]
  // No restructuring needed - CSS handles layout
}
